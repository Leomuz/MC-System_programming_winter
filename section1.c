patterns used to match characters
function GFGFun () { let str = "Visit geeksforGeeks"; let n = str.search (/GeeksforGeeks/i);
]

//1
casts that pointer to be a pointer to a variable assigned to b
character thta has a pointer
a float number that points to apointer to pointer
//2

//3
#include<stdio.h>  it is a standard library header and looks in the system folders first where the compiler has been installed
void modifyArray(int arr[],int size){ prints a modifield array with arguments of int and size
    arr[0] = 10
}
//5
int num = 5;
int num2 = 15;
int *ptr = &num;
